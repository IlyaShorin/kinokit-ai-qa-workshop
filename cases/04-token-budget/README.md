# Case 04: Token Budget

Token and cost comparison case for live workshop discussion.

Use:

```bash
pnpm token-meter
```

The first version reads example JSON reports from case folders and prints a compact comparison table.
